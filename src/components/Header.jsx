import React from "react";

const Header = () => {
  return (
    <nav className="navbar navbar-dark bg-black">
      <div className="container-fluid">
        <span className="navbar-brand mb-0 h1">
          Contact List React + Reducer
        </span>
      </div>
    </nav>
  );
};

export default Header;
